extends layout

block content
    -var now = new Date();
    h1 #{title} / 2013-#{now.getFullYear()} / (#{total_posts})
    
    ul
        include liste
        

    div.pagination
        input(type='hidden' value='1' id='page')
        a(href='#' class='btn more') Moar